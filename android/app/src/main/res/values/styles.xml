<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ===================================== -->
    <!-- Base Application Theme                -->
    <!-- ===================================== -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ===================================== -->
    <!-- Main App Theme (No Action Bar)        -->
    <!-- ===================================== -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!-- ===================================== -->
    <!-- Splash Screen Theme (Android 12+)     -->
    <!-- ===================================== -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">

        <!-- Splash background -->
        <item name="windowSplashScreenBackground">#0F5EA8</item>

        <!-- Use launcher icon (generated by Image Asset) -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>

        <!-- No background circle behind icon -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <!-- App theme after splash -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>

    </style>

</resources>